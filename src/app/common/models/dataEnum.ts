export interface dataEnum {
    id: number;
    text: string;
}

export interface dataStringEnum {
    id: string;
    text: string;
}

export interface dataBooleanEnum {
    id: boolean;
    text: string;
}